name: Temporary B4 formatter

on:
  push:
    branches:
      - feat/modern-b4-transactional-commerce

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Check out branch
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09

      - name: Set up Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: modern/package-lock.json

      - name: Install locked dependencies
        working-directory: modern
        run: npm ci --ignore-scripts

      - name: Format maintained B4 authority
        working-directory: modern
        run: npm exec -- prettier --write src test

      - name: Commit canonical formatting
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git add modern/src modern/test
          if git diff --cached --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore(b4): format transactional commerce"
          git push origin HEAD:feat/modern-b4-transactional-commerce
