# B7 immutable bundle qualification note

This carrier is complete only when the exact branch HEAD passes the permanent backend quality, full-stack runtime, recovery rehearsal and immutable-release-bundle contract gates. The main-branch publication artifact is intentionally created only by the full-stack push run after merge; pull requests prove the package/reload round-trip without publishing a release artifact.
